<div class='entry_pagination'><%= link_to('&#x2190; ранее', user_url(current_site, prev_day_path(:year => @time.year, :month => @time.month, :day => @time.mday)), :class => 'entry_paginate_prev') unless current_site.first_public_entry_at.to_date == @time.to_date %><%= link_to('позже &#x2192;', user_url(current_site, next_day_path(:year => @time.year, :month => @time.month, :day => @time.mday)), :class => 'entry_paginate_next') unless current_site.last_public_entry_at.to_date == @time.to_date %></div>