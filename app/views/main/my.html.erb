<% @title = 'моё' %>
<div class='onair'>
	<%= paginate @entries, :css_class => 'top_nav_paginator' if @page > 1 %>

	<% @entries.each do |entry| %>
		<%= render :partial => 'entries/entry_display', :locals => { :entry => entry, :show_comments_bubble => true, :show_username => true, :show_tags => false, :comment_views => @comment_views } %>
	<% end %>

	<%= paginate @entries %>
</div>

<script type='text/javascript'>
  var inf_page = 1;

  jQuery('.onair').infinitescroll({
    navSelector: '.entry_pagination',
    nextSelector: 'div.entry_pagination a.entry_paginate_prev',
    itemSelector: 'div.post_body',
    debug: false,
    loadingImg: 'http://assets0.mmm-tasty.ru/images/blank.gif',
    loadingText: '<em>Загружаем ...</em>',
    donetext: '<em>На этом все, спасибо.</em>'    
  }, function(items) {
    inf_page += 1;

    ga_event('Infinite Scroll', 'My', current_user ? _user.url : 'anonymous', inf_page);

    // reinitialize fancybox 
    items.each(function(obj, i) { if(current_user) { enable_services_for_current_user(); } jQuery(obj).find('a.fancybox').fancybox({ centerOnScroll: true, hideOnContentClick: true, showNavArrows: false, enableKeyboardNav: false }); });
  });

</script>

<% content_for :sidebar_right do %>
  <div class='section'>
    <h2><span>Сортировать</span></h2>
    <div class='vertical_nav'>
			<%= link_to 'по последнему комментарию', { :action => :my, :page => nil }, :class => (params[:action] == 'my') ? 'highlight' : nil %><br/>
			<%= link_to 'по дате написания', { :action => :last_personalized, :page => nil }, :class => (params[:action] == 'last_personalized') ? 'highlight' : nil %>
		</div>		
  </div>

  <div class='section'>
    <h2><span>Об этом разделе</span></h2>
    
    <div>
      <p>На этой странице появляются все записи, которые могут быть вам интересны: записи людей, на которых вы подписаны и ваши собственные записи.</p> <br/><br/>
      <p>Записи сортируются <span class='strong'>по последнему комментарию</span>, и в первую очередь вы всегда будете видеть места, где идет активное обсуждение.</p>
    </div>
  </div>
  
  <div class='section'>
    <h2><span>Переписки</span></h2>
    
    <div>
      <%
      unviewed_count = current_user.conversations.unviewed.count
      if unviewed_count > 0
      %>
  	    <p>У вас <%= link_to "#{unviewed_count.pluralize('непрочтенная переписка', 'непрочтенных переписки', 'непрочтенных переписок', true)}", user_url(current_user, unviewed_conversations_path) %>.</p>
  	  <% else %>
  	    <p>У вас нет непрочтенных сообщений.</p>
  	  <% end %>
  	  <br/><br/>
      <p><%= link_to 'Все ваши переписки &rarr;', user_url(current_user, conversations_path) %></p>
    </div>
  </div>
<% end %>